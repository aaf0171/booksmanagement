info:
  name: Login Alice DUPONT
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8082/api/auth/login
  body:
    type: json
    data: |-
      {
         "username": "alicedupont",
         "password": "Uc!9x@3arMsM2!!J"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const data = res.getBody();

        const token = data.accessToken;

        bru.setEnvVar("jwt", data.accessToken);
        bru.setEnvVar("refreshToken", data.refreshToken);

        console.log(token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
