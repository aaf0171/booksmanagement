info:
  name: Me
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8082/api/auth/login
  body:
    type: json
    data: |-
      {
         "username": "jc.dusse",
         "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
