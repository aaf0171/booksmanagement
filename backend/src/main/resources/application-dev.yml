spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/booksmanagement
    username: bookadmin
    password: cerise
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
        transport:
          ssl:
            enable: false
  # sql:
  #   init:
  #     mode: always
  #     schema-locations: classpath:schema.sql
  #     data-locations:

app:
  activation:
    email:
      from: noreply@example.com
      subject: "Activate your account"
      base-url: http://localhost:3000
